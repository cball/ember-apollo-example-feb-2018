#import '../fragments/repo-info'

query($login: String!, $repoLimit: Int = 20) {
  organization(login: $login) {
    login
    avatarUrl
    repositories(
      first: $repoLimit
      orderBy: { field: STARGAZERS, direction: DESC }
    ) {
      nodes {
        ...RepoInfo
      }
    }
  }
}
