#import '../fragments/repo-info'

query($login: String!) {
  organization(login: $login) {
    login
    avatarUrl
    repositories(first: 20, orderBy: { field: STARGAZERS, direction: DESC }) {
      nodes {
        ...RepoInfo
      }
    }
  }
}
